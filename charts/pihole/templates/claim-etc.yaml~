apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-etc-pv-claim
  namespace: {{ .Values.namespace }}
spec:
  accessModes: [ ReadWriteOnce ]
  resources: { requests: { storage: 1Gi } }

